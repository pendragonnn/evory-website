<x-app-layout>
    <div class="p-10">
        <h1 class="text-3xl font-bold">{{ $event->event_name }}</h1>
        <p class="mt-2 text-gray-700">{{ $event->description }}</p>
    </div>
</x-app-layout>
